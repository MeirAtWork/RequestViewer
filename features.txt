clicking on a line - line highlighted
hovering on a line - the bachground is highlighted
line number
match braces
search panel (highligth matches, up\down buttons, x of y label, close button)
collapsible anchors
auto linker meaning if the text is a link - its clickable
copy to clipboard button
auto format and indent (= if the string is a single raw json - it displays it in several lines)
semi transparent vertical lines that make the user understand the start and the end of each node
read only
--
colored stack
copy to clipboard button
scrollable

---

left:
 collapse mode should:
  still show the end bracket and
  the ... and
  make the ... clickable and
  change mouse pointer on that ... and
  highlight the line as monaco does

bug:
 search up \ down buttons should be disabled until search term in inserted
 anchor make the numbers crazy - see at the buttom - they are cut of the bottom
 anchor push numbers aside thenit comes to above 100 lines
 second ctrl+f goes to the entire page instead of the viewer

tech dept:
 css file has errors - info not inside css classes
 export the class using one method instead of using the 'window'
 publish as npm package